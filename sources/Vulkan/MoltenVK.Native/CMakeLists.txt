# TODO: CMake is probably not needed here
# Silk 2 uses a fetchDependencies script from the MoltenVK repo and uses make instead
# https://github.com/dotnet/Silk.NET/blob/main/build/nuke/Native/MoltenVK.cs
# However, MoltenVK does provide a CMakeLists file, so maybe use that instead?
